name = "MemSim"
root_directory = "../testbench"

[make]
run_make = true
use_stdout = true

[submissions]
archive_filename = '../submissions.zip'
archive_type = "zip"
submission_type = "tar.gz"
program = "memSim"

[tests]
test_directory = '../tests'
